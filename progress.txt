PROD-READINESS АУДИТ construct-fin — журнал прогресса
Ветка: audit/hardening (от v6 @ f4765ab). НЕ пушить в v6 (триггерит prod-deploy).
План: ~/.claude/plans/velvety-giggling-wozniak.md

=== Фаза 0 — Подготовка инфры (ЗАВЕРШЕНА 2026-06-20) ===
- Ветка audit/hardening создана от v6 @ f4765ab.
- Colima + docker: construct-v6-postgres healthy на :5433 (creds construct/construct_dev_change_me).
- БД construct_v6 и construct_v6_test существуют; test мигрирована (18 миграций, 0 pending).
- Prisma client сгенерирован; @construct/db и @construct/shared собраны в dist/.
- ВАЖНО: корневой .env принадлежит ДРУГОМУ проекту (Python miniapp). Реального apps/api/.env нет.
  Env для прогонов задаётся вручную:
    DATABASE_URL=postgresql://construct:construct_dev_change_me@127.0.0.1:5433/construct_v6?schema=public
    TEST_DATABASE_URL=...construct_v6_test?schema=public
    JWT_SECRET=test-jwt-secret-please-change-0123456789
    TZ=UTC
- ЭТАЛОННЫЙ ПРОГОН (зелёный, базовая линия):
    Unit (api):        216 passed (23 файла)
    Integration (api): 293 passed (20 файлов)
    Web typecheck:     OK
    Web build:         OK (26 маршрутов)

=== Фаза 1 — Статический аудит (4 ветки, Workflow) — СЛЕДУЮЩАЯ ===
Ветки: A ядро/деньги · B конкурентность/tx/идемпотентность · C безопасность/multi-tenant · D флоу/API/фронт.
Реестр дефектов → docs/audit-2026-06-20.md. Реестр тест-кейсов → tests.json.

=== Фаза 2 — Динамические тесты === (В РАБОТЕ)
Инфраструктура готова:
- apps/api/src/loadtest/load-harness.ts — реальный listen :4100 + fetch-клиент (call).
- apps/api/src/loadtest/invariants.ts — независимая сверка инвариантов (склад=Σдвижений, paidAmount=Σпроводок, ship<=qty, return<=ship, переводы, COGS>=0, балансы).
- apps/api/src/loadtest/concurrent-load.loadtest.ts — 4 сценария (смешанная нагрузка + 3 burst).
- vitest.loadtest.config.ts + script test:loadtest.

SMOKE-ПРОГОН (20-40 ops/воркер) ВЫЯВИЛ 2 ДЕФЕКТА-КАНДИДАТА (классификация/фикс — Фаза 4):
  [LT-1] finalize → 500 Internal server error под конкуренцией (30× из ~100 ops).
         Причина: InsufficientStockError (кастомный класс, wavg.ts) НЕ маппится в
         all-exceptions.filter.ts:68 → падает в дженерик 500. Когда finalize дотгружает
         остаток, а общий товар распродан конкурентами → 500 вместо 400/409. Severity: High.
  [LT-2] РАЗРЕШЁН (2026-06-28) — это НЕ потеря платежа и НЕ прод-баг, а дефект ИЗОЛЯЦИИ ТЕСТА.
         Гипотеза «req.body нестабилен» ОПРОВЕРГНУТА фактом: in-memory дамп интерцептора
         показал у всех 12 запросов ИДЕНТИЧНЫЙ requestHash и полный body (bodyType=object).
         Корень: resetDb (money-harness.ts) делал TRUNCATE без таблицы IdempotencyKey. У неё
         нет FK → CASCADE её не чистил → захардкоженный ключ loadtest-idem-key-0001 ПРОТЕКАЛ
         между прогонами. url хэша содержит свежие ws/order id (новые после resetDb), поэтому
         2-й и последующие прогоны видели старую строку с ДРУГИМ requestHash → честный 409
         "другой запрос", проводок=0. Первый прогон на свежей БД всегда зеленел (отсюда
         «гейзенбаг» с console.log — лог-прогон был первым). Интерцептор КОРРЕКТЕН.
         ФИКС: в resetDb добавлены IdempotencyKey + (для явности) StockMovement/Transfer/
         AccountBalanceCheck. Проверка: Сценарий 4 8/8 зелёных (проводок=1, 1×200+11×409
         "ещё выполняется"); 293 integration зелёные; Сценарии 2/3/4 зелёные.
  [LT-1] РАЗРЕШЁН (2026-06-28) — причина НЕ InsufficientStockError (гипотеза опровергнута
         дампом исходного исключения). Реально: finalize — единственный body-less POST, а
         load-harness ВСЕГДА слал content-type: application/json → Fastify бросал
         FST_ERR_CTP_EMPTY_JSON_BODY (statusCode 400), а all-exceptions.filter не уважал
         готовый statusCode → дженерик 500. Прод НЕ затронут: фронт (apps/web/src/lib/api.ts)
         ставит content-type ТОЛЬКО при наличии тела, finalize шлётся без него.
         ФИКС (2 слоя): (1) all-exceptions.filter — ошибки с statusCode 4xx (FastifyError)
         отдаются своим кодом, не маскируются 500; (2) load-harness.call ставит content-type
         только при body (как прод api.ts). Проверка: Сценарий 1 5×5xx=0, нарушений=0;
         все 4 loadtest-сценария зелёные; 216 unit + 293 integration зелёные.
  ЗЕЛЁНЫЕ инварианты (держатся): Сценарий 2 (paidAmount синхронен при 30 оплатах),
         Сценарий 3 (нет oversell при 40 отгрузках на сток 10) — ручные FOR UPDATE работают.

=== Фаза 2 — ЗАВЕРШЕНА (2026-06-28, кроме Playwright→Фаза5) ===
- Нагрузка 5×1000 (5000 ops): Сц1 5xx=0 нарушений=0; Сц2/3/4 нарушений=0, idem проводок=1. ЗЕЛЁНО.
- Функциональные мутации: 49/49 эндпоинтов покрыты, 101 тест в 13 файлах (apps/api/src/functional/),
  vitest.functional.config.ts + script test:functional. Конвенция: buildHttpApp (реальные guards+
  ZodPipe+filter) → inject → проверка statusCode + факт БД через Prisma. Диапазоны telegramId по
  доменам (2100000n..2630000n) для изоляции в общей тест-БД.
- НАЙДЕН+ИСПРАВЛЕН [LT-3]: supplier-return при returnQty>остаток → 500 вместо 400. Причина = тот же
  класс, что LT-1: warehouse.service.ts:361 бросал InsufficientStockError (extends Error) НАПРЯМУЮ,
  фильтр не маппит в 4xx. Фикс: обёрнут в BadRequestException (как decrementForSale:266). Это РЕАЛЬНЫЙ
  прод-баг (нагрузочный тест его не покрывал — не звал supplier-return с превышением). Полнота:
  оставшийся throw в wavg.ts:53 защищён catch в decrementForSale.
- Playwright (Фаза 2.3) — отложен в Фазу 5 (UI после фиксов бэка).
- Регрессия всего пакета ЗЕЛЁНАЯ: 216 unit + 293 integration + 101 functional + 4 loadtest.

=== Фаза 4 / Дорожка R — rollout 11 High статического аудита — ЗАВЕРШЕНА (2026-06-28) ===
ВАЖНО про реестр: summary в docs/audit-phase1-registry.json ВРЁТ (Critical:0/High:11), а top-level
поле severity завышено (Critical:4/High:24). Истинный источник = verdict.severity (после адверс.
верификации 3 голосами): High:11/Med:19/Low:32. Все 11 High верифицированы против ТЕКУЩЕГО кода.
Все 11 закрыты, каждый с тестом, весь пакет зелёный:
  R1 multi-tenant: WorkspaceGuard не проверял Workspace.deletedAt → soft-deleted ws оставался рабочим.
     Фикс: include workspace.deletedAt в findUnique членства (1 запрос). + functional-тест.
  R2 security: CSV formula injection (escapeCsv не нейтрализовал =+-@ TAB CR). Фикс: neutralizeFormula
     префиксует "'" для text-колонок (по kind, числа/минус не трогаем). + unit-тест.
  R3 flow: экспорт отчётов сломан — .strict()-схемы отвергали query-параметр format. Фикс: исключить
     format из rawQuery перед parse во всех ветках kind. + functional-тест.
  R4 money: parseAmount (a) гнал деньги через float Number/toFixed → Decimal toMoneyString half-up;
     (b) разделители 1000× для US "1,234". Политика «асимметрия по локали» (согласована с А.):
     запятая+3цифры→тысячи, точка одиночная→всегда десятичная (1.005=1.01). + unit-тесты.
  R5 money: скидка заказа — отрицательная раздувала total, >subtotal давала отрицательный. Фикс:
     NonNegativeMoneyString для discountAmount + gt(discount,subtotal)→400 в create/update/recompute.
  R6 idempotency (АРХИТЕКТУРНЫЙ, выбран полный фикс): completedAt писался отдельной tx ПОСЛЕ коммита
     домена → краш в окне → ретрай дублировал платёж. Дизайн-проход (3 Plan-агента) → синтез: новый
     TransactionalContext (ALS + generic commit-hook), UoW.drainCommitHooks ВНУТРИ $transaction пишет
     completedAt атомарно с деньгами; interceptor оборачивает подписку в ALS, complete→cacheResponse
     (responseBody + fallback). БЕЗ миграции. completedAt=null теперь надёжно = «деньги не прошли»,
     поэтому lease/expiry-recovery стали корректны. Layer 2 (DB partial-unique на Transaction) —
     ОТЛОЖЕН (требует миграции, отдельный шаг). + 4 теста атомарности (краш-симуляция, ALS, rollback).
     ПРИМ: cheap-review советовал try-catch в хуке — ОТКЛОНЕНО (вернуло бы баг: деньги без маркера).
  R7 frontend: мутации order/purchase/transaction/import не инвалидировали reports/trade-reports/
     summary/warehouse/reconciliation/accounts → устаревшие деньги на экранах. Фикс: дополнена
     инвалидация React Query в 4 хуках. web typecheck зелёный.
Каждый фикс прогнан через дешёвого (Haiku) субагента-ревьюера. Финальный пакет ЗЕЛЁНЫЙ:
  232 unit + 297 integration (+4 атомарности) + 115 functional + 4 loadtest. typecheck api+web чисто.

=== Medium (19) статического аудита — ОБРАБОТАНЫ (2026-06-28) ===
Из 19 по verdict.severity: 3 уже закрыты дорожкой R7 (M14/M15/M19 — FE-инвалидация),
1 отложен в F0 (M1 applySupplierReturn avgCost — WAVG-специфика, устранится при FIFO),
1 отложен как FE-фича (M17 useShip/useReturn — эндпоинты есть, нет хуков/UI; отдельный FE-проход).
ИСПРАВЛЕНО 14 (параллельные субагенты + 2 cheap-review):
  TZ/UTC+5: M2 YoY календарный сдвиг + кламп 29 фев (cheap-review поймал half-fix); M3 reconciliation
    asOf→endOfDay; M8 transaction summary startOfDay/endOfDay; M4 (FE) periods.ts rangeFor в UTC+5.
  import: M10 дедуп контрагентов по lowercase (DB-бэкстоп=миграция отложена); M11 Math.min/max спред→
    цикл (стек); M12 parseDate buildUtcDate строгая валидация (month/day-overflow→null).
  orders/category: M5 reverseFinalization сбрасывает returnedQty (reopen/cancel/remove); M13 ALLOWED_BUCKETS
    валидация kind↔bucket в create+update.
  infra/concurrency: M9 Prisma transient коды (P2034→409, P2024/P1001/P1002→503, InitializationError→503);
    M7 transfer assertAccounts ВНУТРЬ uow + SELECT FOR UPDATE (deadlock-ordering); M6 stockValue SQL-
    агрегация SUM(qty*avgCost) вместо cap-300.
  FE: M16 useTransfers дополнена инвалидация; M18 idempotency-ключ стабилен на операцию (onMutate/useRef).
Регрессия ЗЕЛЁНАЯ: 246 unit + 305 integration + 119 functional + 4 loadtest. typecheck api+web чисто.
M7 остаточный риск: account.softDelete (M3-guard) не берёт FOR UPDATE — полное закрытие требует правки
  account.service (вне скоупа). M18 design-smell (shared ref при конкурентных mutate) — не воспроизводим
  при retry:0 + сериализации RQ.
TODO дальше: Low(32) в бэклог; M1+M17 (в F0/FE-проход); M10 DB-бэкстоп (миграция); R6 Layer 2 (миграция);
  план 06-24 (F0/FIFO — фундамент склада); Playwright (Фаза 5).

=== Low (32) статического аудита — ОБРАБОТАНЫ (2026-06-28) ===
Метод тот же: read-only триаж против ТЕКУЩЕГО кода (4 параллельных агента) → фиксы по группам
  (4 параллельных агента, файлы не пересекаются) → cheap Haiku-review каждой группы → централизованный прогон.
Из 32 по verdict.severity: 3 УЖЕ ЗАКРЫТЫ попутно — #8 idempotency complete() (архитектурно в R6: completedAt
  commit-hook'ом внутри доменной tx); #28 и #31 (это ОДИН баг — инвалидация кэшей в useImportCommit, закрыт R7
  коммитом 7fed333; в реестре дублирующая запись).
ОТЛОЖЕНЫ 2 (по решению пользователя): #1 cashflow byAccount N+1 (perf-долг, переписывать рабочую SQL-агрегацию
  ради малой выгоды не стали); #20 attachment create-TOCTOU (полное закрытие требует @@unique(workspaceId,hash)
  = миграция, по правилам репо только в v6).
#24 — фактически закрыта 2-уровневым инвариантом категорий (self-parent/дети/kind уже запрещены в update()) — no-op.
ИСПРАВЛЕНО 27:
  Reports/Trade/Export: #2 marginPct не прячет убыток при revenue=0 (отрицательный %); #3 by-product ключ
    нормализован trim+lower (имя для показа — исходное); #4 cashflow-export строка Итого (Decimal); #26 marginPeriod
    частичный период → отклоняется валидацией (refine both-or-none); #27 isoDate строгий regex + calendar-refine
    (2026-13-45/02-30 браковка); дедуп локальной копии isoDate; #32 убрана мёртвая ветка query.isError.
  Core: #7 $transaction { maxWait 5000, timeout 15000 }; #9 assertAccountTx ВНУТРИ uow (addPayment+returnItem,
    TOCTOU accountId); #10 resolvePaymentState для total=0 (paid<0→REFUNDED, =0→PAID, >0→OVERPAID); #11 qty/price>0
    refine; #12 transfer FOR UPDATE + AND isArchived=false (перевод на архивный счёт отклонён); #13 from<=to
    superRefine в list+summary.
  Auth/Att/Import/Cat: #14 причина Telegram-verify в logger.warn, клиенту generic; #15 JSON.parse в try/catch +
    zod-валидация user (id:number); #16 logger.warn при пустом TELEGRAM_ALLOWED_IDS (поведение fail-open НЕ менялось);
    #17/#18 file-validation сигнатуры (HEIC/HEIF: ftyp@4 И бренд@8; WEBP: RIFF@0 И WEBP@8) — MP4/WAV больше не проходят;
    #19 дедуп при remove считает hash+workspaceId (без миграции); #21 неизвестный PDF → BadRequest (GENERIC_PDF нет,
    тихий parseWbPdf убран); #22 очистка валютных кодов (RUB/USD/EUR/грн/руб); #23 matcher учитывает kind
    (протянут в import.service); #25 inn regex ^(\d{10}|\d{12})$.
  Shared+FE: #5 parseAmountInput через Decimal (заведён decimal.js-light в packages/shared, ROUND_HALF_UP зеркалит
    бэк; bundle-нейтрально — уже тянется recharts); #6 fromLocalDateInput фиксирует UTC+5 через tzInstant (как M4);
    #29 COGS/прибыль/маржа заказа на shared-Decimal (Number убран, Prisma.Decimal во фронт не тянется); #30 удаление
    чека через ConfirmDialog (паттерн отмены заказа).
В прогоне всплыл 1 межфайловый ассерт: transfers.e2e.integration.test.ts ждал старый текст ошибки #12 —
  3 ассерта обновлены под "not found or archived in this workspace" (поведение корректно).
Регрессия ЗЕЛЁНАЯ: 300 unit + 16 web-unit + 305 integration + 119 functional + 4 loadtest (5×1000, наруш.=0).
  typecheck api+web чисто. Всё в рабочем дереве (НЕ закоммичено — ждёт явного решения).
TODO дальше: коммит Low-пачки в audit/hardening; затем F0/FIFO (план 06-24); миграции в v6 (M10 + R6 L2 + #20);
  M17 + Playwright (FE-проход); решение о мёрже audit/hardening (push в v6 = прод-деплой).
