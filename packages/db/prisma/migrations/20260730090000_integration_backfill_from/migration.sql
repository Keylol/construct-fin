-- Перезалив истории: с какой даты тянуть выписку.
--
-- Адаптеры стартовали строго с даты создания подключения (решение №15 генплана:
-- «историю до подключения занесли руками»). Для перезалива это не годится —
-- банк хранит операции годами (Альфа 5 лет, Т-Банк с июня 2023), а достать их
-- было нечем.
--
-- Null сохраняет прежнее поведение (старт с connectedAt), поэтому существующие
-- подключения миграция не меняет.
--
-- Миграция написана вручную (не через `migrate dev`), чтобы не тянуть в неё
-- DROP INDEX для partial-unique индексов — см. «Известные грабли» в CLAUDE.md.
ALTER TABLE "IntegrationConnection" ADD COLUMN "backfillFrom" TIMESTAMP(3);
