-- Режим правила: POST — проводит строку выписки сразу (прежнее поведение,
-- остаётся умолчанием), SUGGEST — только подставляет статью, строка остаётся
-- во «Входящих» на разбор. Нужен для поставщиков, у которых один платёж
-- раскладывается по-разному (ДНС: часть в сборки, часть в офис).
ALTER TABLE "Rule" ADD COLUMN "mode" TEXT NOT NULL DEFAULT 'POST';
