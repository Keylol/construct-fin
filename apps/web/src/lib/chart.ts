/**
 * Палитра графиков — единый источник, согласованный с дизайн-токенами.
 * recharts принимает CSS-цвета, поэтому семантические серии берём прямо из
 * токенов (hsl(var(--…))), а категориальную палитру держим сдержанной, но
 * различимой (нужно для pie/мульти-линий), с якорем на брендовый navy.
 */

/**
 * Доход/расход/итог — единственные серии отчётных графиков (FlowChart).
 * Линия итога — брендовый синий, осветлённый под тонкую линию: чистый navy
 * токена (#14395d) валидатор dataviz бракует как «читается серым» рядом с
 * осями. Пара зелёный/красный на CVD-границе (ΔE 7.4 protan) — это законно
 * при вторичном кодировании: столбцы всегда в одном порядке (приход слева),
 * с зазором и легендой.
 */
export const CHART_SEMANTIC = {
  income: 'hsl(var(--success))',
  expense: 'hsl(var(--destructive))',
  total: '#2a6cad',
} as const;

/**
 * Категориальная палитра (счета, категории, контрагенты). Первый — брендовый
 * navy, затем янтарь и зелёный из токенов, далее сдержанные различимые оттенки.
 */
export const CHART_PALETTE = [
  'hsl(var(--primary))', // navy
  'hsl(var(--warning))', // янтарь
  'hsl(var(--success))', // зелёный
  '#0E7490', // тил
  '#475569', // графит
  '#9D174D', // глубокая роза
  '#0891B2', // циан
  '#6D28D9', // индиго
  '#A16207', // олива
  '#B91C1C', // кирпич
] as const;

/**
 * Категориальная палитра для ЗАЛИВОК (donut/стеки): 7 слотов, порядок
 * ФИКСИРОВАН (цвет следует за сущностью, не за рангом) и прогнан через
 * валидатор dataviz: lightness band, chroma floor, CVD worst-adjacent
 * ΔE 24.9, contrast ≥3:1 к светлой поверхности. Не переставлять на глаз.
 * Всё сверх 7 серий сворачивается в «Прочее» нейтральным серым.
 */
export const CHART_CATEGORICAL = [
  '#2a6cad', // синий (бренд, осветлён под заливку)
  '#b35309', // янтарь
  '#0f7143', // зелёный
  '#6d28d9', // индиго
  '#a16207', // олива
  '#0891b2', // циан
  '#c02565', // роза
] as const;

/** Слот «Прочее» — нейтральный серый, вне категориальной палитры. */
export const CHART_OTHER = '#94a3b8';
