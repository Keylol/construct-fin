/**
 * Палитра графиков — единый источник, согласованный с дизайн-токенами.
 * recharts принимает CSS-цвета, поэтому семантические серии берём прямо из
 * токенов (hsl(var(--…))), а категориальную палитру держим сдержанной, но
 * различимой (нужно для pie/мульти-линий), с якорем на брендовый navy.
 */

/** Доход/расход (и приглушённые варианты для сравнения периодов). */
export const CHART_SEMANTIC = {
  income: 'hsl(var(--success))',
  expense: 'hsl(var(--destructive))',
  incomeMuted: 'hsl(var(--success) / 0.4)',
  expenseMuted: 'hsl(var(--destructive) / 0.38)',
} as const;

/**
 * Категориальная палитра (счета, категории, контрагенты). Первый — брендовый
 * navy, затем янтарь и зелёный из токенов, далее сдержанные различимые оттенки.
 */
export const CHART_PALETTE = [
  'hsl(var(--primary))', // navy
  'hsl(var(--warning))', // янтарь
  'hsl(var(--success))', // зелёный
  '#0E7490', // тил
  '#475569', // графит
  '#9D174D', // глубокая роза
  '#0891B2', // циан
  '#6D28D9', // индиго
  '#A16207', // олива
  '#B91C1C', // кирпич
] as const;
